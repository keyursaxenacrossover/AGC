h2><p>{code:sql}<br>SELECT iur.user_id, iur.uuid as id, u.u_firstname as first_name, u.u_name as last_name, u.u_email as email<br>FROM users u LEFT JOIN iam_user_reference iur ON u.uid = iur.user_id<br>WHERE u.u_account != "root" AND iur.idp_status = 'STAGED'<br>{code}<br>-&gt; all admin user that are stored in iam_user_reference  with status STAGED<br>-&gt; every user will be activated on okta with the option to send E-Mail to User</p><h2 id="header-5">[4] Portal ClientGroup migration?</h2><p>{code:sql}<br>SELECT p.*, ips.value AS client_id FROM portals p<br>LEFT JOIN iam_portal_settings ips ON p.id=ips.portal_id<br>WHERE ips.portal_id IS NOT NULL<br>{code}<br>-&gt; search in iam_portal_settings  Tabelle for all existing portal application on okta side<br>-&gt; in every application remove the group 'everyone' and add the group iam_client_group_id</p><h2 id="header-6">[5] User ClientGroup migration?</h2><p>{code:sql}<br>SELECT uuid<br>FROM iam_user_reference<br>UNION<br>SELECT uuid<br>FROM iam_portal_user_reference<br>{code}<br>-&gt; try to add all uuid from admin and portals to add to the iam_client_group_id group on Okta</p><h2 id="header-7">[6] Convert admin user from <a href="http://questback.com">questback.com</a> to <a href="http://tivian.com">tivian.com</a> domain</h2><p>identify tivian user with questback.com  E-Mail Address by whitelist.</p><p>user has following options:</p><h3 id="header-9">[0] Back</h3><p>back to main menu</p><h3 id="header-10">[1] List <a href="http://questback.com">questback.com</a> users</h3><p>list users in questback domain filtered by whitelist</p><h3 id="header-11">[2] List <a href="http://tivian.com">tivian.com</a> users</h3><p>list all users in tivian domain.</p><h3 id="header-12">[3] List conflicts</h3><p>shows users that has same local-part and different domain-part in their email address. On left side are user with questback domains and on right side are users with tivian domain. </p><h3 id="header-13">[4] Convert</h3><ol>
<li><p>get uuid for tivian domain from okta → user will be created if not exists </p></li>
<li><p>add new uuid to client  group nad user group</p></li>
<li><p>remove old uuid from client group and user group <strong>(if email address is not used by panelist)</strong></p></li>
<li><p>replace uuid in iam_user_reference table</p></li>
</ol><h2 id="header-15">[7] Convert panelists from <a href="http://questback.com">questback.com</a> to <a href="http://tivian.com">tivian.com</a> domain</h2><p>identify tivian user with <a data-card-appearance="inline" href="http://questback.com">http://questback.com</a>   E-Mail Address by whitelist.</p><p>user has following options:</p><h3 id="header-17">[0] Back</h3><p>back to main menu</p><h3 id="header-18">[1] List <a data-card-appearance="inline" href="http://questback.com">http://questback.com</a>  users</h3><p>list panelists in questback domain filtered by whitelist</p><h3 id="header-19">[2] List <a data-card-appearance="inline" href="http://tivian.com">http://tivian.com</a>  users</h3><p>list all panelistsin tivian domain.</p><h3 id="header-20">[3] List conflicts</h3><p>shows paneliststhat has same local-part and different domain-part in their email address. On left side are user with questback domains and on right side are users with tivian domain. </p><h3 id="header-21">[4] Convert</h3><ol>
<li><p>get uuid for tivian domain from okta → user will be created if not exists </p></li>
<li><p>add new uuid to client  group nad user group</p></li>
<li><p>remove old uuid from client group and user group <strong>(if email address is not used by user)</strong></p></li>
<li><p>replace uuid in iam_user_portal_reference table</p></li>
</ol><h2 id="header-23">[8] Portal update (e.g. update redirects after creation of new domain-alias)</h2><p>Can be used to update portal-applications so the redirect-links are up-to-date if new domain was created.</p><h2 id="header-24">[9] Exit</h