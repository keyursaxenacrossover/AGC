><p></p><h3 id="header-11">External Identity Provider</h3><p>External Identity providers could be connected to the Authentication system to provide SSO accross different systems e.g. customer systems.</p><p></p><h2 id="header-13">Technical Context</h2><img src="https://discoverxi-supportdesk.zendesk.com/hc/article_attachments/20717190756370" alt="Architecture Diagrams - System Context - Technical Context(2).png"><ac:rich-text-body><p><strong>Integration</strong></p>
<p>The integration in other systems is not part of this architecture. This will be handled in the respective documentation of the integrated system. Just the interface to integrate are integral part of this architecture.</p></ac:rich-text-body><p></p><h3 id="header-15">Users</h3><p>Users that want to authenticate at the system. They will use a Browser to access the system that is using the OIDC protocoll.</p><p></p><h3 id="header-16">External Identity Provider</h3><p>External identity providers that could be connected via OIDC oder SAML2 to provide SSO capabilities with external systems.</p><p></p><h3 id="header-17">EFS + Portals + Panelwebsite + MySight</h3><p>The monolithic EFS system also containing different other modules like Portals, Panelwebsite and Mysight and also includes the EFS admin area. The authentication will use OIDC.</p><p>This system might be the leading system for user data. This has to be clarified. It is expted that data from such systems will be delivered via Kafka.</p><p></p><h3 id="header-18">Analytics (the name of the system is not entirely clear)</h3><p>A system that shows and manages result data. It contains the Tivian Analytics/reporting preparation part and the Dashboard part. Both should authenticate using OIDC.</p><p></p><h3 id="header-19">Datavoyager</h3><p>A system to create offline reports that will also connect via OIDC.</p><p></p><h3 id="header-20">Tableau</h3><p>A third-party software that can be connected via OIDC to handle authentication.</p><p></p><h3 id="header-21">GSCC (CXI)</h3><p>A software currently running only in the US to handle communication. It could also be connected via OIDC.</p><p></p><h3 id="header-22">Email</h3><p>An internal email server that can be used to send emails to users. This could for example use SMTP but also an HTTP API might be possible.</p> system also containing different other modules like Portals, Panelwebsite and Mysight and also includes the EFS admin area. The authentication will use OIDC.</p><p>This system might be the leading system for user data. This has to be clarified. It is expted that data from such systems will be delivered via Kafka.</p><p></p><h3 id="header-18">Analytics (the name of the system is not entirely clear)</h3><p>A system that shows and manages result data. It contains the Tivian Analytics/reporting preparation part and the Dashboard part. Both should authenticate using OIDC.</p><p></p><h3 id="header-19">Datavoyager</h3><p>A system to create offline reports that will also connect via OIDC.</p><p></p><h3 id="header-20">Tableau</h3><p>A third-party software that can be connected via OIDC to handle authentication.</p><p></p><h3 id="header-21">GSCC (CXI)</h3><p>A software currently running only in the US to handle communication. It could also be connected via OIDC.</p><p></p><h3 id="header-22">Email</h3><p>An internal email server that can be used to send emails to users. This could for example use SMTP but also an HTTP API might be possible.</p>